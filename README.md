### str-to-list-words-count
## This is a challenge question.
# Write a program that prints the following string and converts the string into a list. Prints the words along with their number of occurrences.
